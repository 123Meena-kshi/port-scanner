# Network Port Scanner

This project is a Python-based network port scanner that scans a target host to detect open ports.

## Features
- Scans ports from 1 to 1024
- Uses multithreading for faster scanning
- Detects open ports on a target system

## Technologies
- Python
- Socket Programming
- Multithreading

## Usage
Run the script using:

python port_scanner.py
